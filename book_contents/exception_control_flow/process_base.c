#include <sys/types.h>
#include <sys/wait.h>
#include <unistd.h>
#include <stdlib.h>

// pid_t getpid(void); 获取当前进程
// ptd_t getppid(void); 获取当前进程的父进程
// void exit(int status);
// pid_t fork(void);
// pid_t waitpid(pit_t pid, int *statusp, int options); 调用 waitpid 使子进程终止或者停止



